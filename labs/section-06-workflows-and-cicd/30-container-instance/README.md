# 30 — Azure Container Instance via Terraform

ACI is the fastest way to run a container in Azure — no orchestration. This lab runs a
public nginx container, reachable on port 80, in a single resource group.
