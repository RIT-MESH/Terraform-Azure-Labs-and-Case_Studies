# 01 — Azure Web App

A Linux App Service running a Node container on the Free/Basic tier. App Service is the
managed PaaS for web apps — no VM, no OS patching, easy scaling.

This lab creates:
- a Service Plan (`azurerm_service_plan`)
- a Linux Web App (`azurerm_linux_web_app`)
