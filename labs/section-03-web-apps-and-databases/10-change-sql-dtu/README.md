# 10 — Change the SQL DTU model (assignment)

Self-check: parameterise the database SKU with a variable and scale by changing tfvars
(`Basic` → `S0` → `S1`). Watch `terraform plan` show the SKU change as an in-place update.
