# 05 — Deployment slots — create

Slots let you stage a new build and warm it up before swapping it into production. This
lab creates a web app **plus** a `staging` slot. (Slots require a Basic or higher plan.)
