# 15 — Mini project — MySQL server

Azure Database for MySQL (Flexible Server) is a managed MySQL 8 instance. This lab
creates a Burstable B1ms server with public access, a firewall rule for Azure services,
and an admin login from sensitive variables.
