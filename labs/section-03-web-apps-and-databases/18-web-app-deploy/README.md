# 18 — Mini project — deploy the Azure web app

Deploy a web app that points at the MySQL database. The connection string is built from
the MySQL server (re-declared here for completeness) and stored in `app_settings`.
