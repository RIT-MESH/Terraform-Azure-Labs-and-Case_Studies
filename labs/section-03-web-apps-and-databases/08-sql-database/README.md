# 08 — Azure SQL Database

Create a logical SQL Server and a single database on the cheapest DTU tier (Basic, 5
DTU). The admin password comes from a sensitive variable. Create the server then the
database as a separate resource.
