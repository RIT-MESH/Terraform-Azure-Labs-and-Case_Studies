# terraform.tfvars is AUTO-LOADED by `terraform plan/apply`. It supplies values
# for the declared variables without cluttering the CLI. Use -var-file for
# alternate files (e.g. prod.tfvars).
location    = "eastus"
name_prefix = "sttfvar"
tier        = "Standard"
