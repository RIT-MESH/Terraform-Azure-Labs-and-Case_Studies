# 07 — Modules — public IP and network interface

A module that, given an existing subnet, creates a public IP + NIC. Shows how a module
consumes a resource id produced upstream (subnet from the vnet module).
