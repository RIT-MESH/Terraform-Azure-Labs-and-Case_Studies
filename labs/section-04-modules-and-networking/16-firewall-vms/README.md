# 16 — Azure Firewall — virtual machine setup

Hub-spoke: a hub VNet with the `AzureFirewallSubnet`, plus a workload VM in a spoke.
Lab 17 deploys the firewall; labs 18-20 add routing, NAT and application rules.
