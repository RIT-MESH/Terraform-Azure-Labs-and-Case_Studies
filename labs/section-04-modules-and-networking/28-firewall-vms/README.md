# 28 — Azure Firewall — virtual machine setup

Hub-spoke: a hub VNet with the `AzureFirewallSubnet`, plus a workload VM in a spoke.
Lab 29 deploys the firewall; labs 30-32 add routing, NAT and application rules.
