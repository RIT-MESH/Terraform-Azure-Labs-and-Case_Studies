# 12 — Landing Zone — logging

Centralise logs in one Log Analytics workspace in the security RG, plus a storage account
for long-term archive. Diagnostic settings (added per resource) point at this workspace.
